<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
        name="info"
        type="com.example.ybuyanova.weatherapp.WeatherInfo" />
        <variable
            name="temp"
            type="com.example.ybuyanova.weatherapp.WeatherInfo.Temp" />
        <variable
            name="desc"
            type="com.example.ybuyanova.weatherapp.WeatherInfo.WeatherDescription" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        >

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@={info.city}"
            android:textColor="@android:color/holo_blue_light"
            android:textSize="30sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@={temp.temp}"
            android:textColor="@android:color/holo_blue_dark"
            android:textSize="24sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@={desc.description}"
            android:textColor="?attr/colorPrimary"
            android:textSize="24sp" />

    </LinearLayout>
</layout>
